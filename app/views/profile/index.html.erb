<div class="container" id="pending-uois">

<% if current_user %>
  <% if @ious.any? %>
    <ul>
    <% @ious.each do |iou| %>
      <li>
        <%= iou.title %>
        <%= iou.name %>
        <%= iou.amount %>
        <%= link_to "Accept", iou_path(iou), method: :patch %>
        <%= iou.status %>
      </li>
    <% end %>
    </ul>
  <% else %>
    <p>NO IOUS!</p>
  <% end %>
<% end %>

<%= render "uoi_button" %>

</div>
